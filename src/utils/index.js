
/**
 * @template {import('./public.js').YADWOptions} T
 * @param {T} config
 * @returns {T}
 */
export function defineConfig(config) {
    return config;
}

/**
 * @template {import('./public.js').YADWPresences} T
 * @param {T} presence
 * @returns {T}
 */
export function definePresences(presence) {
    return presence;
}
